# Lemon System Version
ADDITIONAL_BUILD_PROPERTIES += \
    ro.lemon.version=$(LEMON_VERSION) \
    ro.lemon.build.version=$(PLATFORM_VERSION)

# Lemon Display Version
ADDITIONAL_BUILD_PROPERTIES += \
    ro.lemon.display.version=$(LEMON_DISPLAY_VERSION)
